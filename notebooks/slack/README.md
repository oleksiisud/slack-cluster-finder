Current implementation assumes key is static and given. It must be connected with the authentication process to take the key.

workspaces.html contains the extraction site, where the user can choose which messages to extract.

text_extract_animation contains an animation. 